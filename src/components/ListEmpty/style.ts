import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    container: {
        borderTopWidth: 1,
        marginTop: 20,
        borderTopColor: "#333333",
    },
    containerContent: {
        marginTop: 48,
        alignItems: 'center',
    },
    text: {
        marginTop: 16,
        color: "#808080"
    }
})